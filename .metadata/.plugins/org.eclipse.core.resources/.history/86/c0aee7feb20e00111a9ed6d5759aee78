package com.semanticsquare.GenericsDemo;

interface Operation<T, T1, T2> {
	  void performOperation(T1 firstVal, T2 secondVal, T a);
}
public class GenericsPractice {
	
	
	public static void main(String[] args) {
		Operation<String> operate = new OperationPerformed<>();
		
		operate.performOperation("*");
	}
}


class OperationPerformed<T, T1, T2> implements Operation<T, T1, T2> {
	
		T a;
		T1 b;
		T2 c;
		
	@Override
	public void performOperation(T1 firstVal, T2 secondVal, T a) {
		// TODO Auto-generated method stub
		switch (a) {
		case "*": {
			
			System.out.println("You selected multiplication" b * c);
		}
		default:
			throw new IllegalArgumentException("Unexpected value: " + a);
		}
		System.out.println("I m passing genric value" + a);
	}
	
}
